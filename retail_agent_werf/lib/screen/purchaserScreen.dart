import 'package:flutter/material.dart';

class PurchaserScreen extends StatelessWidget {
  const PurchaserScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("PurchaserScreen"),
    );
  }
}
